# Write a program in Python to remove repetitive items from a list.

num=[2,3,4,5,2,6,3,2]
x=[]
for i in range(len(num)):
    if num[i] not in x:
      x append(num[i])
    else:
        pass
print(x)        
        
